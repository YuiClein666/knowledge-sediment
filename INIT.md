# INIT · 初始化一座知识库

> **这是给 Agent 读的指令，不是给用户读的文档。**
> 用户只需要说一句"帮我初始化知识库"，剩下全是你的事。
>
> 你的任务不是"建几个空文件夹"，而是**把这个人会什么、在做什么，变成一座可检索的知识库**。

---

## 为什么不能套模板

市面上大多数笔记系统的初始化，是让人自己建目录。结果通常是：建了 12 个文件夹，
三个月后 11 个是空的——因为**分类不是用户能凭空想出来的东西**。

所以反过来做：**让 Agent 从用户已有的素材里推导出分类，再让用户改。**
给选项比让用户填空容易得多——改一个已有的方案，永远比从零想一个简单。

**推论：不同的人应该得到不同的分类。** 做前端的人不需要 MySQL 单元，
做数据的人不需要 CSS 布局。**如果你的输出和上一个用户的输出一样，说明你偷懒了。**

---

## 第 1 步：确认工作区与工具

```bash
pwd                      # 确认当前工作区
ls                       # 看有什么
python --version         # 确认有 Python（工具是纯标准库，3.8+ 即可）
```

确认你手上工具库的位置（本仓库），后面脚本都从它调用：

```bash
TOOLKIT="<本仓库的绝对路径>"     # 例如 C:/Users/you/knowledge-sediment
```

> ⚠️ **路径一律用真实绝对路径**（Windows `C:\...`、macOS/Linux `/Users/...`）。
> 不要用 `~` 或相对路径——不同工具解析不一致，这是最常见的初始化失败原因。

---

## 第 2 步：引导投喂素材（★ 最关键的一步）

**不要把用户扔给一个空表单。** 用类似这样的话开口：

> "为了建一座对你有用的知识库，我先得知道你会什么、在做什么。
> 下面这些**随便给几样就够**，不用整理、不用归类：
>
> - 简历（PDF、图片、文档都行）
> - 正在做的项目的 README / 设计文档
> - 在学的技术栈、正在上的课、看的书
> - 论文 / 研究方向 / 研究笔记
> - 已有的零散笔记（放在哪都行，哪怕是聊天记录）
>
> 有什么给什么就行。给得越多，分类越准。"

用户给了素材之后，**你负责去读**（不要让他复制粘贴）：

| 素材形态 | 怎么读 |
|---|---|
| PDF | 用 PDF 解析能力提取文本 |
| 图片 | 直接看图（简历截图、架构图都可以） |
| Markdown / 代码目录 | 列目录 + 读关键文件（README、docs/、设计文档） |
| 一个项目文件夹 | 先看结构，再挑 2-3 个核心文件读 |

**如果用户说"没什么素材"**：不要卡住。用 3-5 个问题现场收集：

1. 你现在主要在做什么？（工作 / 学习 / 研究）
2. 最近半年学得最多的是什么？
3. 有没有正在做的项目？做什么的？
4. 未来一年想在哪方面变强？

**绝不要因为缺少素材就产出空模板——那等于没做事。**

---

## 第 3 步：分析领域画像

把素材整理成一份**内部结论**（不需要给用户看全）：

```text
主领域：      <1-3 个，用户投入最多、最可能持续沉淀的>
次领域：      <有涉猎但非主要>
项目/作品：   <有名字的具体项目>
在学中：      <最近半年新增的技能>
研究/论文：   <如果有>
```

判断"主领域"的标准：**他会不会在未来持续产出这方面的知识？**
一次性的、已经结束的事情不是主领域。

---

## 第 4 步：提议分类体系（给用户改）

按下面的原则设计，然后**用表格呈现给用户，明确说"你可以直接改"**：

### 设计原则

| 原则 | 说明 |
|---|---|
| **3-6 个分类** | 少了装不下，多了找不到 |
| **编号承担语义** | `10-` `20-` `30-`，位置本身有序、可插入新分类 |
| **单元 = 可复用的一块知识** | 不是"一件事"。`23-mysql` 是单元，"优化过一次慢查询"不是 |
| **每类 3-6 个单元** | 允许留空单元（作为"这里该放什么"的引导） |
| **用用户的语言** | 他说"八股"就别写成"计算机基础理论" |

### 单元从哪里来（三个可靠来源）

1. **素材里反复出现的主题** → 出现 3 次以上的，几乎必然该有单元
2. **项目里踩过的坑 / 做过的技术决策** → 这类最值得沉淀，因为最难重新推导
3. **在学但还没掌握的东西** → 空单元，作为"往这儿记"的引导

### 不该进知识库的（必须排除）

- ❌ 个人档案类（性格、家庭、情感、经历）→ 那是另一座库的事，混进来会稀释检索
- ❌ 一次性事件（某次会议、某次汇报）
- ❌ 纯待办清单
- ✅ 判据：**"三个月后我还会想再查它吗？"** 会 → 进；不会 → 不进

### 呈现给用户的格式

```markdown
我按你的素材建议这样分类（**可以直接改**——加、删、改名都行）：

| 分类 | 收什么 | 单元 |
|---|---|---|
| `10-<主领域>` | <一句话> | `11-xxx`、`12-xxx`、`13-xxx` |
| `20-<次领域>` | <一句话> | `21-xxx`、`22-xxx` |
| `40-<项目>` | <一句话> | `41-<项目A>`、`42-<项目B>` |
| `60-<工具箱>` | 工具用法与踩坑 | `61-xxx`、`62-xxx` |

留了几个空单元（`13-xxx`），是给你之后往里记的。

有要改的吗？没有我就开始建了。
```

**必须等用户确认或修改后再进第 5 步。** 这是整个流程里唯一需要用户动脑的地方，
也是他对自己知识库产生"所有权感"的时刻——不要跳过他。

---

## 第 5 步：生成骨架

把确认后的方案写成 `taxonomy.json`：

```json
{
  "version": 1,
  "categories": [
    {
      "id": "10-ai-engineering",
      "title": "AI 工程",
      "keywords": ["agent", "prompt", "rag", "llm"],
      "summary": "把大模型能力组装成可用系统的实践经验",
      "units": [
        {
          "id": "11-agent-architecture",
          "title": "Agent 架构",
          "keywords": ["multi-agent", "workflow", "state"],
          "summary": "多智能体协作、工作流编排、状态管理"
        }
      ]
    }
  ]
}
```

然后运行：

```bash
python "$TOOLKIT/scripts/scaffold_kb.py" \
    --taxonomy taxonomy.json \
    --kb "<知识库绝对路径>"
```

脚本会生成分类目录、单元目录，以及每层的 `_index.md` 模板
（含 `title` / `tags` / `keywords` / `summary` 字段——**这些字段是 Agent 检索的入口，不能省**）。

**如果脚本报错**：手动建目录也可以，但必须遵守下面的文件规范。

---

## 第 6 步：写入配置

```bash
python "$TOOLKIT/scripts/install_plugin.py" \
    --target <workbuddy|codebuddy> \
    --kb "<知识库绝对路径>"
```

它做三件事：
1. 装 skill 到 Agent 的技能目录（**立即生效，不用重启**）
2. 注册插件 + hook（**需要重启 Agent**）
3. 写配置到 `~/.knowledge-sediment/config.json`
   （知识库位置 + 分类体系 + Python 路径——**与 Agent 无关的公共位置，换工具不用重配**）

---

## 第 7 步：通用化改造（仅当用户用其他 Agent）

本工具的可移植设计（供适配时参考）：

| 层 | 载体 | 移植难度 |
|---|---|---|
| **声明层** | `SKILL.md` + `AGENTS.md` | ⭐ 放个文件就行 |
| **执行层** | 纯标准库 Python 脚本 | ⭐ 有 Python 就能跑 |
| **触发层** | `hooks/hooks.json` | ⭐⭐⭐ **事件名与配置位置因 Agent 而异** |

**只有触发层需要适配。** 如果目标 Agent 不支持 hook，就退化：
只装 skill（声明层），靠"说一句沉淀一下"手动触发——
**功能不缺，只是少了自动性。** 不要把不支持 hook 当成阻塞。

事件名参考（已实测）：
- WorkBuddy / CodeBuddy：一轮结束 = `FinalStop`（注意不是 `Stop`），注入上下文 = `UserPromptSubmit`

---

## 第 8 步：（可选）关联到 Git

**先问用户要不要**，并且**必须说明可见性风险**：

> "要不要把知识库同步到 GitHub？配了之后换设备、手机端都能接着用。
> **但要注意：知识库是你自己的东西，如果是工作内容，建议用私有仓库。**
> 要配的话我需要你在 GitHub 建一个仓库（或我引导你建）。"

配的话：

```bash
cd "<知识库路径>"
git init
git add index.md _map.md _health.md 00-inbox    # 白名单，不要 git add .
git commit -m "init: 知识库"
git remote add origin <仓库地址>
git push -u origin main
```

> ⚠️ **自动同步 + 公开仓库是危险组合。** 如果用户的仓库是 public，
> 提醒他执行一次敏感信息扫描（密钥、公司内部名称、客户信息）。

---

## 第 9 步：首次引导（决定他会不会真的用）

初始化完成不等于他以后会用。用**三次实际的沉淀**教会他：

1. **示范一次自动沉淀**
   挑刚才对话里一条有价值的内容（比如他讲的项目决策），说：
   "刚才你说的『XXX』我记下来了，放在 `40-projects/41-xxx/`。
   这就是它的工作方式——你不用管，该记的我记。"

2. **教三个动作**

   | 说什么 | 会发生什么 |
   |---|---|
   | "沉淀一下" | 把当前讨论里可复用的部分收进知识库 |
   | "查一下 XX" | 按分类和关键词去知识库找 |
   | 正常聊 | 我自动判断，有价值的顺手记 |

3. **给一个立刻有用的动作**
   让他现在就问一个真实问题（"我之前那个项目的 XX 怎么做的？"），
   你从知识库检索回答——**让他当场体验到"它真的记得"**。

**不要问太多、不要一次讲完机制。** 先让他看到价值，机制可以以后慢慢懂。

---

## 附录 A · 文件规范（违反会导致检索失效）

### 单元描述文件 `<单元>/_index.md`

```markdown
---
title: 单元名（用断言或明确的主题词）
tags: [关键词1, 关键词2, 关键词3]      # ★ Agent 主要靠这个定位
keywords: [同义词, 别名, 英文名]        # 帮助匹配不同说法
summary: 一句话说清"这个单元收什么"
---
```

### 分类描述文件 `<分类>/_index.md`

同上，外加一张"本单元包含"的表格（由 `knowledge_cli.py index` 自动生成）。

### 内容文件 `<单元>/<主题>.md`

```markdown
---
title: 断言式标题（"X 会导致 Y"，不是"关于 X"）
tags: [关键词]
status: draft          # draft（仅本地）→ promoted（已验证可信）→ deprecated（过时）
confidence: medium     # low / medium / high
relations:             # 可选：与其他单元的关系，用于生成引用地图
  requires: [其他单元/文件]
  relates_to: [...]
  supersedes: [...]
---
```

> **标题写成断言**是最容易被忽略、但对检索帮助最大的一条：
> `"B+树让范围查询更高效"` 比 `"B+树"` 好搜得多，也更容易在若干年后被自己理解。

---

## 附录 B · 初始化完成后的自检

```bash
python "$TOOLKIT/scripts/knowledge_cli.py" stats    # 应看到分类与单元数
python "$TOOLKIT/scripts/knowledge_cli.py" index    # 生成各层索引
python "$TOOLKIT/scripts/knowledge_cli.py" health   # 结构健康度
```

- `stats` 分类数为 0 → 配置没写对，检查 `~/.knowledge-sediment/config.json`
- `health` 出现大量"空单元" → 正常，这是引导用的坑位，不是错误
- `map` 无关系 → 正常，关系是沉淀过程中逐渐长出来的

**不要为了"看起来完整"而给空单元塞占位内容**——空洞的条目比空目录更有害，
因为它会占用检索结果、稀释可信度。

---

## 附录 C · 一条底线

初始化过程中你可能会读到用户的隐私信息（简历里的电话、项目里的公司内部资料）。
**不要把它们写进知识库的通用单元**，也不要写进任何会同步到公开仓库的地方。

知识库记录的是**"他会什么"**，不是**"他是谁"**、更不是**"他在哪工作过"**。
这条线模糊了，这座库就失去了给别人看、给自己查的价值。
